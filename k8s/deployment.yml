apiVersion: apps/v1
kind: Deployment
metadata:
  name: doc-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: doc-ai
  template:
    metadata:
      labels:
        app: doc-ai
    spec:
      containers:
      - name: doc-ai
        image: doc-ai:latest
